75 HARD TRACKER - DEPLOYMENT CHECKLIST
======================================

□ STEP 1: REVOKE OLD TOKEN (IMPORTANT!)
   → Go to: https://github.com/settings/tokens
   → Find your exposed token (ending in ...6Y0qcLX)
   → Click "Delete" to revoke it

□ STEP 2: CREATE NEW GITHUB TOKEN
   → Go to: https://github.com/settings/tokens
   → Click "Generate new token (classic)"
   → Name: 75hard-tracker
   → Scope: Check "repo" (full control)
   → Click "Generate token"
   → COPY THE TOKEN (you won't see it again!)
   → Token: gh_________________________________

□ STEP 3: CHANGE ADMIN PASSWORD
   → Open: admin.html
   → Find line 195: const ADMIN_PASSWORD = 'Kandarp@75hard';
   → Change to your secure password
   → Save file

□ STEP 4: PUSH TO GITHUB
   → Run these commands:
   
   cd /path/to/your/75hard/folder
   git add .
   git commit -m "Setup automated tracker"
   git push origin main

□ STEP 5: DEPLOY TO VERCEL
   → Go to: https://vercel.com
   → Sign in with GitHub
   → Click "Add New Project"
   → Import your "75hard" repo
   → Add Environment Variables:
     • GITHUB_TOKEN = [paste your token from step 2]
     • GITHUB_USER = gajveenastores-dotcom
     • GITHUB_REPO = 75hard
   → Click "Deploy"
   → Wait for deployment to complete

□ STEP 6: TEST IT!
   → Public site: https://your-project.vercel.app/
   → Admin panel: https://your-project.vercel.app/admin.html
   → Try logging in and updating data
   → Check if public site shows your update

DONE! 🎉

Your live URLs:
• Public: https://_________________.vercel.app/
• Admin:  https://_________________.vercel.app/admin.html

Share the public URL with anyone - they can see your progress!
Keep the admin URL private - that's for updates only.
